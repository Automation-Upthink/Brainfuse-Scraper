package com.upthink;public class UniqueAccountSubject {    private String accountNumber;    private String subject;    public UniqueAccountSubject(String accountNumber, String subject) {        this.accountNumber = accountNumber;        this.subject = subject;    }    // Getters    public String getAccountNumber() {        return accountNumber;    }    public String getSubject() {        return subject;    }    // Setters (optional, if you need to modify the fields later)    public void setAccountNumber(String accountNumber) {        this.accountNumber = accountNumber;    }    public void setSubject(String subject) {        this.subject = subject;    }    // Override equals and hashCode to ensure uniqueness in sets    @Override    public boolean equals(Object o) {        if (this == o) return true;        if (o == null || getClass() != o.getClass()) return false;        UniqueAccountSubject that = (UniqueAccountSubject) o;        if (!accountNumber.equals(that.accountNumber)) return false;        return subject.equals(that.subject);    }    @Override    public int hashCode() {        int result = accountNumber.hashCode();        result = 31 * result + subject.hashCode();        return result;    }    @Override    public String toString() {        return "{" +                "\"accountNumber\":\"" + accountNumber + '\"' +                ", \"subject\":\"" + subject + '\"' +                '}';    }}