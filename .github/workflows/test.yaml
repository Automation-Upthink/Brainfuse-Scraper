name: Test Workflow From GSheet

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Workflow successfully triggered"

      - name: Show date
        run: date
